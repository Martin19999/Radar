import { mode } from "@chakra-ui/theme-tools";
import { theme as baseTheme } from "@chakra-ui/react";


const Form = {
  baseStyle: (props) => ({
    container: {
      mt: 5,
      fontSize: ["sm","md"], 
    }
  }),
}

export default Form;